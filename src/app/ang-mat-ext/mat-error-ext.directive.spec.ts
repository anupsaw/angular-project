import { MatErrorExtDirective } from './mat-error-ext.directive';

describe('MatErrorExtDirective', () => {
  it('should create an instance', () => {
    const directive = new MatErrorExtDirective();
    expect(directive).toBeTruthy();
  });
});
